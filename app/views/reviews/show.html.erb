
  <h2><%= @review.title %></h2>
<br>
  <h3><%= @review.body %></h3>

  <% @review.comments.each do |comment| %>
  <%= comment.created_at %> by <%= comment.user.email %>
  <br>
  <%= comment.body %>
  <% end %>

<%= render 'comments/form' %>

<%= link_to 'Back', reviews_path %>
